import styled from 'styled-components';

const StyledWrapper = styled.div`
  display: flex;
  align-items: center;
  flex-flow: column;
  color: white;
  padding-top: 2rem;

  h1 {
    font-weight: 100;
  }

  h1:last-of-type {
    padding-bottom: 2rem;
  }

`;

export default StyledWrapper;