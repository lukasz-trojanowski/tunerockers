import styled from 'styled-components';

const CloseIcon = styled.span`
  cursor: pointer;
  line-height: 1;
  font-size: 2rem;
  position: absolute;
  right: 10px;
  top: 0px;
`;

export default CloseIcon;